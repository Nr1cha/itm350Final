on: push
jobs:
  firstFinal-job:
    runs-on: ubuntu-latest
    steps:
      - run: node --version
      - run: npm --version
      - run: pwd
      - run: ls
      - run: cd .. 
      - run: npm install
      - run: node app.js